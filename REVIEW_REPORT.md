# تقرير المراجعة الشاملة — نظام تأجير المعدات الثقيلة (Heavy Equipment ERP)

## إجابات مباشرة على الأسئلة

### هل الأقسام مرتبطة ببعضها محاسبياً بشكل سليم؟
بعد الإصلاح: نعم بشكل أساسي. التربيطات الحالية:
- العميل ← عقود ← فواتير ← دفعات/سندات قبض ← كشف الحساب.
- المعدة ← عقود ← فواتير ← مصروفات ← سندات صرف/قبض موجهة للمعدة.
- الحسابات البنكية/الخزينة ← عهد (فتح/تعزيز/إغلاق/مشتريات) وسندات قبض/صرف.
- الموظف ← سلفيات/خصومات ← مسيرات ← مدفوعات رواتب.
- الفاتورة ← بنود وخصومات ← طباعة وتصدير.

### هل اختبرت الوحدات؟
لم أتوسع في كتابة أتمتة وحدات رسمية، لكن تم فحصهما بـ:
- `tsc` لكل من main و web: `0 errors`.
- `npm run build`: نجح بالكامل.
- مراجعة منطق المعالجات باليد.
- لم تُكتب اختبارات اختراق جدولية (unit/component/e2e) بعد.

### هل الإعدادات جاهزة للربط بـ Cloudflare؟
الإعدادات جاهزة لكن فيها عالمان:
- `VITE_API_BASE` يرتبط بـ `About.tsx`.
- `LICENSE_SECRET` يختم أكواد التفعيل.
- `CLOUDFLARE_SETUP.md` + `.env.example` يوضعان التعليمات.
- يجب عليك لملمة القيم الحقيقية قبل الرفع.

### هل النظام جاهز للربط ببوت التليجرام المرتبط بـ Cloudflare Webhook؟
نعم البروتوكول موجود:
- `worker.js` يستقبل `/telegram-webhook`.
- الأوامر: `/start` `/about` `/set_about` `/set_support` `/set_version` `/set_status` `/generate_code <client> <days> <device_id>`.
- فقط `ADMIN_TELEGRAM_ID` يستطيع التنفيذ.
- `/api/about` تقرأ من KV وتظهر في صفحة «حول».
- يبقى عليك رفع `worker.js` وضبط متغيرات البيئة وربط Webhook بالتوكن.

### هل الفاتورة احترافية في الإنشاء والتصدير؟
- الإنشاء: بنود متعددة، ضريبة، عملة، خصومات، حالة، تاريخ استحقاق، طبع بقالات (Modern/Classic/Compact/Elegant/Thermal).
- التصدير: أُضيف `exceljs` وزر تصدير Excel في صفحة الفواتير.
- إعدادات طباعة كاملة (قالب/ورق/اتجاه/لون/خط/عناصر تظهر) في الإعدادات.
- ما تزال هناك قوالب تستخدم الصيغة `en`؛ من الأفضل ضبطها `ar` لاحقاً لاحترافية أعلى.

### هل سندات الصرف يمكن ربطها بالمعدات؟
نعم، أُضيف جدول `vouchers` وواجهة «السندات» وقسم في التقرير للمعدات (`voucher_expenses`). يمكن ربط سند الصرف بمعرّف المعدة ويرصد في تقرير المعدات.

### هل سندات القبض يمكن ربطها بعميل؟
نعم، سند القبض يحمل `client_id` ويمكن ربطه بفاتورة وبنك/خزينة وتصديره ويظهر في السندات.

### هل سندات الصرف والقبض تغطي جميع العمليات المحاسبية التي يمكن أن أجريها؟
كجزء من النظام: نعم في التغطية الحالية (سندات مباشرة + ارتباط بالعملاء/المعدات/الفواتير/الخزائن/الطباعة/التصدير/تقارير).
لا تزال تفتقد: سندات ربط تلقائي بـ«العهد/الرواتب»، وسندات ترحيل تلقائي عند إغلاق العهد (تُنشأ يدويًا حالياً).

### هل المنطق المحاسبي سليم؟
بعد الإصلاح:
- إعادة احتساب رصيد العميل ودفعات الفاتورة من المصادر الأصلية.
- استبعاد الضريبة من الإيرادات (`total_before_vat`).
- ترقيم المستندات بـ `MAX(id)+1`.
- منع الحذف مع مراجع (عميل/معدة/موظف/سائق/بنك).
- منع فتح/إغلاق مبالغ غير صحيحة.
- إغلاق العهدة يسوي الزيادة/العجز.
- احتياطي نسخ DB آمن.

### هل المحرك المحاسبي سليم؟
المحرك الحالي «محرك ترحيل بسيط يدوي». متين في الأساس، لكنه ليس محرك دبل/قيود مزدوجة كامل. الحالات الصعبة (مرتجعات، تحويلات بين حسابات، تأجيل ضريبة، قيود ترحيل تلقائية) تحتاج طبقة قوائم محاسبية.

### هل التقارير سليمة؟
- تقارير شركة/معدات/عقود/مصروفات/كشف حسابات/كشف السندات تعرض البيانات المطلوبة.
- أُضيف السندات للإيراد/المصروف في تقرير المعدات.
- يجب فحص الفترات المتقاطعة (مثل الفواتير المدفوعة جزئياً) للتأكد إذا كانت محددة حسب date الفاتورة أم date الدفع.

### هل يوجد قسم لإعدادات الطباعة يعمل بشكل سليم داخل الإعدادات؟
نعم، أُضيف قسم «إعدادات الطباعة» كامل:
- القالب وحجم الورق والاتجاه واللون وحجم الخط.
- خيارات إظهار: الشعار/اسم الشركة/الرقم الضريبي/السجل التجاري/العنوان/الهاتف/التذييل.
- يتم تخزينه في `app_settings.print_settings` ويطبق على الطباعة.

### هل فورم الإدخال لكل قسم يغطي القسم محاسبياً ومنطقياً وعملياً؟
إلى حد كبير:
- عملاء: حد ائتماني + رصيد افتتاحي + رصيد جاري.
- معدات: أسعار ساعة/يوم/شهر + عداد + حالة.
- عقد: عميل/معدة/سائق + عداد + سعر وحدة.
- فاتورة: بنود + ضريبة + خصومات + استحقاق + عملة/قالب طباعة.
- مصروف: معدّة/فئة/تاريخ/مبلغ.
- مشتريات: مورد/بيان/مبلغ/عهدة/تاريخ/حالة.
- عهدة: شخص/بنك/مبلغ وتقيّد يدوي بالخزينة.
- صندوق: عميل/فاتورة/خزينة/مبلغ، وسند قبض يستخدم لدفع فاتورة.
- رواتب: أساسي+إضافي+مكافآت+سلف+خصومات؛ مسيرة تجمع ذلك.
- سندات: صرف/قبض + عميل/معدة/فاتورة/بنك/طريقة/تاريخ.

## ما يجب أخذ الحذر منه
1. **اختبار SQLite الأصلي**: `better-sqlite3` حزمة ربط أصلية تحتاج بيئة بناء مناسبة لتفعيلها. الفحص الحالي تم عبر `tsc` و `build`.
2. **إعدادات الـ Worker**: يجب ضبط `ADMIN_TELEGRAM_ID` و `TELEGRAM_BOT_TOKEN` و `LICENSE_SECRET` أولاً.
3. **خوارزمية الطباعة**: تعديل قوالب HTML لتستخدم `lang="ar"` لكي تكون أصقلياً.
4. **محاسبة أكثر اكتمالاً**: جدول `GL_LEDGER` و `GL_JOURNAL` وقيود مزدوجة تلقائية (تحويلات بنوك، عهدة/مرتب).
5. **قاعدة وحدات**: إضافة اختبارات `vitest`/`jest` وتغطية كل دالة محاسبية.

## التحقق الممتد المنفذ

```bash
npx tsc -p tsconfig.node.json --noEmit
npx tsc -p tsconfig.web.json --noEmit
npm run build
npm audit --omit=dev --audit-level=high
```

- الترجمة: **نجح**
- البناء: **نجح**
- Audit: ثغرات `moderate` في `react-router` و `exceljs/uuid`.
